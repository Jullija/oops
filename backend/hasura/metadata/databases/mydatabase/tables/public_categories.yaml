table:
  name: categories
  schema: public
array_relationships:
  - name: subcategories
    using:
      foreign_key_constraint_on:
        column: category_id
        table:
          name: subcategories
          schema: public
select_permissions:
  - role: student
    permission:
      columns:
        - category_id
        - category_name
        - label
      filter: {}
    comment: ""
