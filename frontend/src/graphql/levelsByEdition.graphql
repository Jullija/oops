query LevelsByEdition($editionId: bigint!) {
  editionByPk(editionId: $editionId) {
    levels {
      grade
      highest
      imageFileId
      label
      levelId
      maximumPoints
      minimumPoints
      name
    }
  }
}
