query getGroup($editionId: bigint!, $groupId: bigint!) {
  groups(
    where: { editionId: { _eq: $editionId }, groupsId: { _eq: $groupId } }
  ) {
    userGroups {
      userId
      user {
        firstName
        secondName
      }
    }
    groupName
  }
}
