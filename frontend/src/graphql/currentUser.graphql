query CurrentUser {
  getCurrentUser {
    nick
    role
    userId
    userGroups {
      group {
        edition {
          editionId
          editionName
          editionYear
          endDate
          label
          startDate
        }
      }
    }
  }
}
