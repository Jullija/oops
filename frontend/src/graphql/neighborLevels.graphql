query neighborLevels($editionId: bigint!, $levelIds: [Int!]!) {
  levels(
    where: { ordinalNumber: { _in: $levelIds }, editionId: { _eq: $editionId } }
  ) {
    ordinalNumber
    imageFileId
    name
    maximumPoints
    minimumPoints
  }
}
