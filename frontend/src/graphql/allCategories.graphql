query AllCategories {
  categories(orderBy: { categoryName: ASC }) {
    categoryId
    categoryName
    categoryEditions {
      editionId
    }
    subcategories(orderBy: { subcategoryName: ASC }) {
      editionId
      subcategoryId
      subcategoryName
      maxPoints
    }
  }
}
