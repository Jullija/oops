query AllCategories {
  categories(orderBy: { categoryName: ASC }) {
    categoryId
    categoryName
    subcategories(orderBy: { subcategoryName: ASC }) {
      editionId
      subcategoryId
      subcategoryName
      maxPoints
    }
  }
}
