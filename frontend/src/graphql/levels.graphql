query Levels($editionId: bigint) {
  levels(where: { editionId: { _eq: 2 } }, orderBy: { ordinalNumber: ASC }) {
    grade
    imageFileId
    levelId
    maximumPoints
    minimumPoints
    name
    ordinalNumber
    label
    highest
    editionId
  }
}
