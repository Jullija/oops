query HallOfFame($editionId: bigint) {
  hallOfFame(
    where: { editionId: { _eq: $editionId } }
    orderBy: [{ sumOfPoints: DESC }, { nick: ASC }]
  ) {
    editionId
    userImageId
    levelId
    levelName
    nick
    sumOfPoints
    userId
    groupsId
    groupName
  }
}
